/**
 * Take Ticket endpoint. Slack Interactivity can point to this URL or /api/slack-interaction.
 * Same handler as slack-interaction for "Take Ticket" button clicks.
 */

import handler from "./slack-interaction.js";

export default handler;
